# 클라우드타입 배포 — 단계별 안내 (PostgreSQL 저장 구조)

> 목표: `www.clearcheolgeo.com` 으로 홈페이지 공개
> 순서: PostgreSQL 준비 → 클라우드타입 배포 → 임시주소로 폰 확인 → 가비아 DNS 연결

---

## ⚠️ 왜 데이터베이스가 반드시 필요한가

클라우드타입 공식 문서에 다음과 같이 명시되어 있습니다.

> "현재 클라우드타입은 디스크 마운트를 지원하지 않아 어플리케이션에서 파일로 출력하더라도
> 영구히 저장되지 않습니다."
> — [클라우드타입 Docs · 일반적인 문제](https://docs.cloudtype.io/guide/troubleshooting/common)

즉 **컨테이너 안에 파일로 저장하면 재시작·재배포 때 고객 문의가 전부 사라집니다.**
그래서 이 프로젝트는 파일 저장 방식을 폐기하고, 접수 내용과 첨부 사진을
**외부 PostgreSQL 에 트랜잭션으로 저장**하도록 구현되어 있습니다.

`DATABASE_URL` 이 없으면 접수를 아예 받지 않고 전화상담 안내를 보여줍니다.
(저장되지 않는데 "접수되었습니다"라고 표시하는 일은 발생하지 않습니다.)

### 클라우드타입이 제공하는 PostgreSQL 을 쓰면 안 되나요?

쓸 수는 있지만 **권장하지 않습니다.** 클라우드타입 문서에도
"동적 확장이나 자동 백업 등 매니지드 데이터베이스 기능을 지원하지 않으며,
데이터 손실 및 기타 우발적인 데이터 관련 사고에 유의"하라고 안내되어 있습니다.
([PostgreSQL 문서](https://docs.cloudtype.io/guide/databases/postgres))

고객 개인정보가 담기는 데이터이므로 **자동 백업이 되는 관리형 PostgreSQL** 을 권합니다.

---

## 0단계. 준비물

| 필요한 것 | 비용 | 비고 |
| --- | --- | --- |
| GitHub 저장소 | 무료 | ✅ 완료 (SUNGJIHOOON/clearcheolgeo) |
| **관리형 PostgreSQL** | 무료 시작 가능 | Supabase 또는 Neon 권장. 자동 백업 여부 확인 |
| 클라우드타입 계정 | 무료 가입 | GitHub 계정으로 로그인 |

> 계정 생성과 결제는 **직접** 하셔야 합니다.

---

## 1단계. PostgreSQL 준비

Supabase 또는 Neon 중 하나에 가입하고 프로젝트(데이터베이스)를 하나 만듭니다.
둘 다 무료 요금제가 있고 자동 백업을 제공합니다. (요금제별 보관 기간은 가입 시 확인)

만들고 나면 **연결 문자열(Connection string)** 을 복사합니다. 이런 형태입니다.

```
postgresql://사용자이름:비밀번호@호스트주소:5432/데이터베이스이름
```

> 📌 **Supabase 를 쓰신다면** 연결 문자열이 여러 개 보입니다.
> 서버에서 접속하는 용도이므로 **Connection pooling / Pooler** 쪽 주소를 쓰는 편이 안전합니다.
> (무료 요금제는 동시 접속 수 제한이 낮습니다.)
>
> 📌 이 문자열에는 **DB 비밀번호가 들어 있습니다.** 채팅·문서에 붙여넣지 마시고,
> 클라우드타입 환경변수 칸에 직접 입력하세요.

**표는 따로 만들 필요 없습니다.** 홈페이지가 처음 실행될 때 필요한 테이블을 자동으로 생성합니다.

---

## 2단계. 클라우드타입에서 배포

1. [cloudtype.io](https://cloudtype.io) → **GitHub 계정으로 로그인**
2. 새 프로젝트 → **GitHub 저장소 연결** → `SUNGJIHOOON/clearcheolgeo` 선택
3. 템플릿(프리셋): **Next.js**

### 애플리케이션 설정 화면에서

| 항목 | 값 |
| --- | --- |
| Node.js version | **22** (그대로 두세요) |
| Build type | 비워두기 |
| Install command | 비워두기 (기본 `npm ci`) |
| Build command | 비워두기 (기본 `npm run build`) |
| Health Check | 비워두기 |

> **볼륨/디스크 설정은 찾지 않으셔도 됩니다.** 이제 파일을 저장하지 않으므로 필요 없습니다.

### 성능(요금제)

Next.js 빌드는 메모리를 꽤 사용합니다. **0.5GB 프리티어에서는 빌드가 실패할 수 있습니다.**
빌드 로그에 `killed` 또는 `out of memory` 가 보이면 **1GB 이상으로 올려서** 다시 배포하세요.

---

## 3단계. 환경변수 입력

`＋` 버튼으로 줄을 추가하며 아래를 넣습니다.

| Name | Value |
| --- | --- |
| `DATABASE_URL` | 1단계에서 복사한 연결 문자열 |
| `ADMIN_USER` | 원하는 아이디 (예: `clearadmin`) |
| `ADMIN_PASSWORD` | **길고 어려운 비밀번호** (16자 이상 권장) |
| `IP_HASH_SALT` | 아무 긴 임의 문자열 (한 번 정하면 변경 금지) |

`NEXT_PUBLIC_SITE_URL` 은 **도메인 연결 후**에 추가하고 재배포합니다.

> ⚠️ `DATABASE_URL` 이 없으면 홈페이지는 정상적으로 뜨지만 **견적문의 폼 대신 전화상담 안내**가 표시됩니다.
> ⚠️ `ADMIN_PASSWORD` 가 없으면 `/admin` 이 503으로 완전히 차단됩니다.

---

## 4단계. 배포 → 폰으로 확인

`xxxxx.cloudtype.app` 임시 주소가 나오면 휴대폰에서 열어보세요.

- [ ] 하단 상담바의 **전화상담** → 통화 화면으로 넘어가는지
- [ ] **견적문의** 접수 → "견적문의가 접수되었습니다" + 접수번호가 나오는지
- [ ] `임시주소/admin` → 아이디·비밀번호 입력창이 뜨고, 방금 넣은 문의가 보이는지
- [ ] `/admin` 상단에 **"저장소: PostgreSQL · 연결 정상"** 으로 표시되는지
- [ ] **재배포를 한 번 더 한 뒤** `/admin` 에 문의가 그대로 남아 있는지 ← 꼭 확인

마지막 항목이 이번 수정의 핵심입니다.

---

## 5단계. 가비아 도메인 연결

### 5-1. 클라우드타입에 도메인 등록

서비스 설정의 **도메인 / 커스텀 도메인** 에서 `www.clearcheolgeo.com` 추가
→ 화면에 "가비아 DNS 에 이 값을 넣으세요"라는 안내(보통 CNAME)가 나옵니다.

### 5-2. 가비아에 DNS 레코드 입력

My가비아 → **DNS 관리툴** → 해당 도메인 → DNS 설정

| 호스트 | 타입 | 값 |
| --- | --- | --- |
| `www` | CNAME | 클라우드타입이 알려준 주소 |

> 최상위 주소(`clearcheolgeo.com`)도 쓰시려면 클라우드타입 안내의 A 레코드 값 또는 포워딩을 이용하세요.
> **임의의 IP 를 적어두지 않은 이유는 서비스마다 값이 다르고 바뀌기 때문입니다.**

### 5-3. 확인

```bash
nslookup www.clearcheolgeo.com 8.8.8.8
```

### 5-4. 마무리

1. 환경변수에 `NEXT_PUBLIC_SITE_URL=https://www.clearcheolgeo.com` 추가
2. **재배포** (이 값은 빌드 시점에 반영됩니다)
3. `https://www.clearcheolgeo.com/sitemap.xml` 생성 확인
4. 구글 서치콘솔 / 네이버 서치어드바이저에 등록

---

## 도메인 현재 상태 (2026-09-08 확인)

`clearcheolgeo.com` 은 공개 DNS(8.8.8.8, 1.1.1.1)에서 **조회되지 않습니다.**
도메인은 보유 중이지만 네임서버/DNS 설정이 비어 있을 가능성이 큽니다.

→ My가비아 **DNS 관리툴**에 도메인이 등록되어 있는지 5단계 전에 확인해 주세요.

---

## 운영 시 주의

- **접수 알림이 자동으로 가지 않습니다.** 문자·이메일 발송 서비스가 연결되어 있지 않아,
  새 문의는 `/admin` 에서 확인하셔야 합니다.
- **DB 자동 백업을 반드시 켜 두세요.** 고객 개인정보가 담긴 유일한 사본입니다.
- 첨부 사진도 DB 에 저장됩니다. 사진 1장 최대 10MB · 접수당 최대 5장이므로,
  **무료 요금제 저장공간(보통 0.5GB 내외)은 사진이 많으면 금방 찹니다.** 사용량을 주기적으로 확인하세요.
- `/admin` 비밀번호는 고객 개인정보를 여는 열쇠입니다. 공유하지 마세요.
- 코드를 수정해 GitHub 에 push 하면 클라우드타입이 자동으로 재배포합니다.
