# 클라우드타입 배포 — 단계별 안내

> 목표: `www.clearcheolgeo.com` 으로 홈페이지 공개
> 순서: GitHub 준비 → 클라우드타입 배포 → 임시주소로 폰 확인 → 가비아 DNS 연결

⚠️ **먼저 읽어주세요**
클라우드타입의 화면 구성과 메뉴 이름, 요금은 바뀔 수 있습니다.
아래는 "무엇을 왜 입력해야 하는지"를 기준으로 정리한 것이며,
실제 버튼 이름이 조금 다르면 **비슷한 항목**을 찾으시면 됩니다.
막히시면 그 화면을 캡처해서 보내주세요.

---

## 0단계. 준비물

| 필요한 것 | 비용 | 비고 |
| --- | --- | --- |
| GitHub 계정 | 무료 | 코드를 보관하는 곳. 클라우드타입이 여기서 코드를 가져갑니다 |
| 클라우드타입 계정 | 무료 가입 | GitHub 계정으로 로그인 가능 |
| 유료 요금제(권장) | 월 5천~1만원대 | **저장 디스크(볼륨)** 때문에 필요. 아래 3단계 참고 |

> 계정 생성과 결제는 **직접** 하셔야 합니다. (대신 해드릴 수 없습니다)

---

## 1단계. GitHub에 코드 올리기

코드는 이미 커밋까지 끝내 두었습니다. **비밀번호·접수데이터는 제외**되어 있습니다.

### 1-1. GitHub 가입 후 저장소 만들기

1. [github.com](https://github.com) 가입
2. 우측 상단 **＋** → **New repository**
3. 입력값
   - Repository name: `clear-demolition`
   - **Private 선택** ← 꼭 Private 로 하세요
   - **Add a README file 은 체크하지 마세요** (비어 있어야 합니다)
4. **Create repository**

### 1-2. 만들어진 주소를 알려주세요

`https://github.com/사장님계정/clear-demolition.git` 형태의 주소가 나옵니다.
이 주소를 알려주시면 제가 업로드 명령을 정확히 만들어 드립니다.

참고로 업로드 명령은 아래 두 줄입니다. (계정 주소만 바꾸면 됩니다)

```bash
git remote add origin https://github.com/사장님계정/clear-demolition.git
```

```bash
git push -u origin main
```

> 처음 push 할 때 GitHub 로그인 창이 뜹니다. 브라우저에서 로그인하시면 됩니다.

---

## 2단계. 클라우드타입에서 배포하기

1. [cloudtype.io](https://cloudtype.io) 접속 → **GitHub 계정으로 로그인**
2. 새 프로젝트 / 새 서비스 만들기
3. **GitHub 저장소 연결** → 방금 만든 `clear-demolition` 선택
   - 처음이면 GitHub 앱 설치 권한 요청이 뜹니다. 해당 저장소만 허용하면 됩니다.
4. 템플릿(프리셋) 선택 화면에서 **Next.js** 또는 **Node.js** 선택
   - Dockerfile 을 쓰는 옵션이 있으면 그걸 골라도 됩니다 (저장소에 이미 들어 있습니다)

### 빌드 설정 (프리셋이 자동으로 채워주면 그대로 두세요)

| 항목 | 값 |
| --- | --- |
| Node 버전 | **20 이상** (22 권장) |
| 설치 명령 | `npm ci` |
| 빌드 명령 | `npm run build` |
| 시작 명령 | `npm run start` |
| 포트 | `3000` |

---

## 3단계. ⭐ 가장 중요 — 저장 디스크(볼륨) 연결

**이 단계를 건너뛰면 고객이 남긴 견적문의가 재배포할 때마다 사라집니다.**

이 홈페이지는 접수 내용과 첨부사진을 서버 폴더에 저장합니다.
클라우드타입 서비스 설정에서 **볼륨 / 스토리지 / 디스크** 항목을 찾아 아래처럼 연결하세요.

| 항목 | 값 |
| --- | --- |
| 마운트 경로 | `/app/data` |
| 용량 | 1GB 로 시작해도 충분 (사진 첨부량에 따라 조정) |

> 무료 요금제에는 영구 디스크가 없거나 일정 시간 후 잠자기가 될 수 있습니다.
> **실제로 고객 문의를 받는 홈페이지라면 유료 요금제 + 볼륨을 권합니다.**
>
> 만약 클라우드타입에서 볼륨을 붙일 수 없다면 알려주세요.
> 접수 내용을 데이터베이스에 저장하도록 제가 코드를 바꿔드리겠습니다. (그대로 두면 유실됩니다)

---

## 4단계. 환경변수 입력

서비스 설정의 **환경변수(Environment Variables)** 에 아래를 등록합니다.

| 이름 | 값 | 설명 |
| --- | --- | --- |
| `ADMIN_USER` | 원하는 아이디 | 접수 목록 보는 계정 |
| `ADMIN_PASSWORD` | **길고 어려운 비밀번호** | 이걸 아는 사람만 고객 문의를 볼 수 있습니다 |
| `IP_HASH_SALT` | 아무 긴 임의 문자열 | 스팸 방지용 (한 번 정하면 바꾸지 마세요) |
| `QUOTE_DATA_DIR` | `/app/data` | 3단계에서 연결한 볼륨 경로와 **똑같이** |
| `NEXT_PUBLIC_SITE_URL` | `https://www.clearcheolgeo.com` | 도메인 연결 후에 넣습니다 (아래 주의) |

> ⚠️ `ADMIN_PASSWORD` 를 넣지 않으면 `/admin` 은 아예 열리지 않습니다(안전한 기본값).
> ⚠️ `NEXT_PUBLIC_SITE_URL` 은 **빌드할 때 적용**됩니다. 값을 넣거나 바꾼 뒤에는 **반드시 재배포**해야 반영됩니다.
> 도메인 연결 전이라면 이 값은 비워두고, 4단계 이후에 넣고 재배포하세요.

---

## 5단계. 배포 → 폰으로 확인

배포가 끝나면 `xxxxx.cloudtype.app` 같은 **임시 주소**가 나옵니다.
휴대폰 브라우저에 그대로 입력하면 홈페이지가 뜹니다.

이때 확인할 것

- [ ] 하단 상담바의 **전화상담** 버튼 → 통화 화면으로 넘어가는지
- [ ] **견적문의** 접수 → "견적문의가 접수되었습니다" + 접수번호가 나오는지
- [ ] `임시주소/admin` 접속 → 아이디·비밀번호 입력창이 뜨고, 방금 넣은 문의가 보이는지
- [ ] 지원금 팝업이 화면 밖으로 안 나가는지

---

## 6단계. 가비아 도메인 연결

### 6-1. 클라우드타입에서 도메인 등록

서비스 설정의 **도메인 / 커스텀 도메인** 에서 `www.clearcheolgeo.com` 을 추가합니다.
그러면 화면에 **"가비아 DNS에 이 값을 넣으세요"** 라는 안내가 나옵니다.
(보통 `CNAME` 값 하나입니다.)

### 6-2. 가비아에서 DNS 레코드 넣기

My가비아 → **DNS 관리툴** → 해당 도메인 → **DNS 설정**

| 호스트 | 타입 | 값 |
| --- | --- | --- |
| `www` | CNAME | 클라우드타입이 알려준 주소 |

> 최상위 주소(`clearcheolgeo.com`, www 없는 주소)도 함께 쓰시려면
> 클라우드타입 안내에 나오는 A 레코드 값 또는 포워딩 설정을 이용하세요.
> **여기 임의의 IP를 적어두지 않은 이유는, 서비스마다 값이 다르고 바뀌기 때문입니다.**
> 반드시 클라우드타입 화면에 표시된 값을 그대로 쓰세요.

### 6-3. 반영 확인

10분~1시간(길면 하루) 걸립니다.

```bash
nslookup www.clearcheolgeo.com 8.8.8.8
```

### 6-4. 마무리

1. `NEXT_PUBLIC_SITE_URL=https://www.clearcheolgeo.com` 환경변수 추가
2. **재배포**
3. `https://www.clearcheolgeo.com/sitemap.xml` 이 생성되는지 확인
4. 구글 서치콘솔 / 네이버 서치어드바이저에 사이트 등록

---

## 지금 도메인 상태 (2026-09-08 확인)

`clearcheolgeo.com` 은 공개 DNS에서 **조회되지 않는 상태**입니다.
도메인은 보유 중(가비아 이용 서비스 1건)이지만 네임서버/DNS 설정이 비어 있을 가능성이 큽니다.

→ **DNS 관리툴에 도메인이 등록되어 있는지** 먼저 확인해 주세요. 6단계 전에 해결되어야 합니다.

---

## 배포 후 운영 시 주의

- **접수 알림이 자동으로 가지 않습니다.** 문자·이메일 발송 서비스가 연결되어 있지 않아,
  새 문의는 `/admin` 에서 직접 확인하셔야 합니다. (알림 연결을 원하시면 말씀해 주세요)
- `/admin` 비밀번호는 고객 개인정보를 여는 열쇠입니다. 공유하지 마세요.
- 접수 데이터 백업 주기를 정해 두세요. (볼륨 스냅샷 또는 주기적 내려받기)
- 코드를 수정하면 GitHub 에 push → 클라우드타입이 자동 재배포합니다.
