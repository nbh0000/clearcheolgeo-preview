# 배포 안내 — www.clearcheolgeo.com 연결

> 도메인: **clearcheolgeo.com** (가비아 등록)
> 2026-09-07 기준, 이 도메인은 공개 DNS에서 조회되지 않는 상태입니다.
> (네임서버 미설정 또는 등록 직후 전파 중으로 보입니다. 가비아 마이페이지에서 도메인 상태를 먼저 확인해 주세요.)

---

## 0. 먼저 알아야 할 것 — 도메인 ≠ 호스팅

도메인은 **주소**일 뿐이고, 홈페이지가 실제로 돌아가려면 **프로그램을 실행해 주는 서버(호스팅)** 가 따로 필요합니다.

이 홈페이지는 **Node.js 로 동작**합니다. (Next.js 서버 렌더링 + 견적문의 접수 API)

- ❌ 일반 웹호스팅(PHP·HTML 전용 상품)에는 **올릴 수 없습니다.**
  → 가비아에서 구매하신 상품이 "웹호스팅(리눅스/PHP)"이라면 이 사이트는 실행되지 않습니다. 상품명을 확인해 주세요.
- ✅ 필요한 것: **Node.js 20 이상을 실행할 수 있는 서버** (클라우드 서버 / 컨테이너 호스팅 / Vercel·Railway 등)

---

## 1. 배포 방식 3가지

### A. 서버형 호스팅 (권장 — 지금 코드 그대로)

가비아 클라우드 서버, AWS Lightsail, Railway, Cloudtype 등 **Node.js 를 직접 실행**할 수 있는 환경.

- 장점: **접수 데이터를 서버 폴더에 저장하는 현재 방식이 그대로 동작**합니다. 사진 첨부도 그대로 됩니다.
- 필수 조건: 접수 데이터를 저장할 **영구 디스크(볼륨)** 와 정기 백업.

Docker 를 지원하는 곳이면 저장소에 포함된 `Dockerfile` 을 그대로 사용할 수 있습니다.

```bash
docker build -t clear-demolition .
```

```bash
docker run -d -p 3000:3000 -v /srv/clear-data:/app/data --env-file .env.production --name clear clear-demolition
```

Docker 없이 서버에 직접 올리는 경우:

```bash
npm ci && npm run build && npm run start
```

> 이때는 `pm2` 같은 프로세스 매니저로 상시 실행시키고, nginx 로 80/443 → 3000 포트를 연결한 뒤 무료 SSL(Let's Encrypt)을 발급하세요.

### B. Vercel (무료로 시작 가능, 저장소 교체 필요)

Next.js 를 만든 회사의 서비스라 배포는 가장 쉽지만, **파일 저장이 유지되지 않습니다.**
그대로 올리면 접수 내용이 배포/재시작 때마다 사라집니다.

→ 올리기 전에 `lib/storage.ts` 를 데이터베이스(예: Supabase Postgres) + 파일 스토리지 연동으로 교체해야 합니다. (작업 필요)

### C. 정적 호스팅 (불가)

가비아 웹호스팅, S3 정적 웹사이트 등에는 **올릴 수 없습니다.** 견적문의 접수가 서버 없이 동작하지 않습니다.

---

## 2. 가비아에서 도메인 연결하기 (DNS 설정)

도메인은 가비아에 그대로 두고, **DNS 레코드만** 홈페이지가 돌아가는 서버 쪽으로 향하게 하면 됩니다.

**경로:** My가비아 → 서비스 관리 → 도메인 → 해당 도메인 → **DNS 정보 / DNS 레코드 관리**

> 가비아 네임서버(ns.gabia.co.kr 등)를 사용 중일 때의 방법입니다.
> 다른 곳의 네임서버로 바꾸셨다면 그쪽 관리화면에서 동일한 레코드를 넣으면 됩니다.

### 서버 IP 가 있는 경우 (A 방식)

| 호스트 | 타입 | 값 |
| --- | --- | --- |
| `@` | A | 서버 공인 IP (예: `123.123.123.123`) |
| `www` | A | 서버 공인 IP (동일) |

### 배포 서비스가 주소를 알려주는 경우 (Vercel·Railway 등)

| 호스트 | 타입 | 값 |
| --- | --- | --- |
| `www` | CNAME | 서비스가 알려주는 주소 (예: `xxxx.vercel.app`, `xxxx.up.railway.app`) |
| `@` | A 또는 ALIAS | **서비스 대시보드에 표시되는 값 그대로** 입력 |

> ⚠️ 최상위 도메인(`@`)에는 원칙적으로 CNAME 을 넣을 수 없습니다.
> 배포 서비스 화면에 안내되는 A 레코드 IP 또는 ALIAS 값을 그대로 쓰세요.
> 여기 예시 IP 를 임의로 적어두지 않은 이유는, 서비스마다 값이 다르고 바뀔 수 있기 때문입니다.

### 설정 후 확인

DNS 반영에는 보통 10분~1시간(길면 하루) 걸립니다. 반영 여부는 아래로 확인합니다.

```bash
nslookup www.clearcheolgeo.com 8.8.8.8
```

---

## 3. 도메인 연결 후 반드시 할 설정

### (1) 사이트 주소 환경변수

서버의 환경변수에 아래를 추가해야 canonical 주소, 공유 미리보기, `sitemap.xml` 이 정상 생성됩니다.

```
NEXT_PUBLIC_SITE_URL=https://www.clearcheolgeo.com
```

설정 전까지는 가짜 주소를 만들지 않도록 sitemap 이 비어 있습니다.

### (2) 나머지 필수 환경변수

```
ADMIN_USER=관리자아이디
ADMIN_PASSWORD=길고_추측_어려운_비밀번호
IP_HASH_SALT=아무_긴_임의문자열
QUOTE_DATA_DIR=/app/data          # 영구 볼륨 경로
```

`ADMIN_PASSWORD` 를 설정하지 않으면 `/admin` 은 503으로 완전히 차단됩니다(안전한 기본값).

### (3) HTTPS 필수

견적문의로 이름·연락처·주소가 오가므로 **반드시 https 로 서비스**해야 합니다.
서버형이면 Let's Encrypt 무료 인증서, Vercel·Railway 는 자동 발급됩니다.

### (4) www 통일

`www.clearcheolgeo.com` 과 `clearcheolgeo.com` 중 **하나를 대표 주소로 정하고 나머지는 리다이렉트**하세요.
(검색엔진 중복 색인 방지) 대표 주소는 `NEXT_PUBLIC_SITE_URL` 과 일치시켜야 합니다.

### (5) 배포 후 점검

- [ ] `https://www.clearcheolgeo.com` 접속 확인 (자물쇠 표시)
- [ ] 휴대폰에서 하단 상담바 · 전화 버튼 동작 확인
- [ ] 견적문의 실제 접수 → `/admin` 에서 조회 확인
- [ ] 접수 데이터 폴더가 영구 볼륨에 저장되는지, 백업이 도는지 확인
- [ ] `https://www.clearcheolgeo.com/sitemap.xml` 생성 확인
- [ ] 구글 서치콘솔 / 네이버 서치어드바이저에 사이트 등록
