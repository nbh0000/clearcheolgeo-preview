# 배포 안내 — www.clearcheolgeo.com 연결

> 클라우드타입으로 배포하신다면 **`CLOUDTYPE.md` 를 보세요.** 이 문서는 일반적인 배포 안내입니다.
>
> 도메인: **clearcheolgeo.com** (가비아 등록)
> 2026-09-08 기준 이 도메인은 공개 DNS(8.8.8.8, 1.1.1.1)에서 조회되지 않습니다.
> 네임서버/DNS 설정이 비어 있을 가능성이 큽니다. My가비아 **DNS 관리툴**에서 먼저 확인해 주세요.

---

## 0. 먼저 알아야 할 것

### 도메인 ≠ 호스팅

도메인은 **주소**일 뿐이고, 홈페이지가 실제로 돌아가려면 **프로그램을 실행해 주는 서버**가 따로 필요합니다.
이 홈페이지는 **Node.js 20 이상**에서 동작합니다. (Next.js 서버 렌더링 + 견적문의 접수 API)

- ❌ 일반 웹호스팅(PHP·HTML 전용 상품)에는 올릴 수 없습니다.
- ✅ Node.js 를 실행할 수 있는 서버 / 컨테이너 호스팅 / Vercel 등이 필요합니다.

### 데이터베이스가 반드시 필요합니다

접수 내용과 첨부 사진은 **외부 PostgreSQL 에 저장**됩니다.
컨테이너 파일시스템에는 아무것도 저장하지 않으므로, 재배포해도 데이터가 사라지지 않습니다.

`DATABASE_URL` 이 설정되지 않으면 견적문의 폼 대신 전화상담 안내가 표시되고 서버도 접수를 거부합니다.
(저장되지 않는데 접수된 것처럼 보이는 일은 발생하지 않습니다.)

**자동 백업이 되는 관리형 PostgreSQL** 을 쓰세요. (Supabase, Neon, AWS RDS 등)

---

## 1. 배포 방식

### A. 컨테이너 / 서버형 호스팅

클라우드타입, Railway, 가비아 클라우드 서버, AWS Lightsail 등.

Docker 를 지원하는 곳이면 저장소의 `Dockerfile` 을 그대로 쓸 수 있습니다.

```bash
docker build -t clear-demolition .
```

```bash
docker run -d -p 3000:3000 --env-file .env.production --name clear clear-demolition
```

Docker 없이 서버에 직접 올리는 경우:

```bash
npm ci && npm run build && npm run start
```

> 이때는 `pm2` 같은 프로세스 매니저로 상시 실행시키고, nginx 로 80/443 → 3000 포트를 연결한 뒤
> 무료 SSL(Let's Encrypt)을 발급하세요.

### B. Vercel

접수 내용을 외부 PostgreSQL 에 저장하므로 Vercel 에도 그대로 배포할 수 있습니다.
다만 서버리스 환경은 커넥션이 자주 새로 열리므로, DB 제공자의 **커넥션 풀러(pooler) 주소**를 사용하고
`DATABASE_POOL_MAX` 를 작게(1~3) 두세요.

### C. 정적 호스팅 (불가)

가비아 웹호스팅, S3 정적 웹사이트 등에는 올릴 수 없습니다. 견적문의 접수가 서버 없이 동작하지 않습니다.

---

## 2. 가비아에서 도메인 연결하기 (DNS 설정)

도메인은 가비아에 그대로 두고, **DNS 레코드만** 홈페이지가 돌아가는 서버 쪽으로 향하게 하면 됩니다.

**경로:** My가비아 → 서비스 관리 → 도메인 → 해당 도메인 → **DNS 관리툴 / DNS 설정**

### 서버 IP 가 있는 경우

| 호스트 | 타입 | 값 |
| --- | --- | --- |
| `@` | A | 서버 공인 IP |
| `www` | A | 서버 공인 IP (동일) |

### 배포 서비스가 주소를 알려주는 경우 (클라우드타입·Vercel 등)

| 호스트 | 타입 | 값 |
| --- | --- | --- |
| `www` | CNAME | 서비스가 알려주는 주소 |
| `@` | A 또는 ALIAS | **서비스 대시보드에 표시되는 값 그대로** |

> ⚠️ 최상위 도메인(`@`)에는 원칙적으로 CNAME 을 넣을 수 없습니다.
> 배포 서비스 화면에 안내되는 A 레코드 또는 ALIAS 값을 그대로 쓰세요.
> **임의의 IP 를 미리 적어두지 않은 이유는 서비스마다 값이 다르고 바뀌기 때문입니다.**

### 반영 확인

DNS 반영에는 보통 10분~1시간(길면 하루) 걸립니다.

```bash
nslookup www.clearcheolgeo.com 8.8.8.8
```

---

## 3. 환경변수

```
DATABASE_URL=postgresql://사용자:비밀번호@호스트:5432/DB이름
ADMIN_USER=관리자아이디
ADMIN_PASSWORD=길고_추측_어려운_비밀번호
IP_HASH_SALT=아무_긴_임의문자열
NEXT_PUBLIC_SITE_URL=https://www.clearcheolgeo.com
```

- `DATABASE_URL` 이 없으면 온라인 접수가 중단되고 전화상담 안내가 표시됩니다.
- `ADMIN_PASSWORD` 가 없으면 `/admin` 은 503으로 완전히 차단됩니다(안전한 기본값).
- `NEXT_PUBLIC_SITE_URL` 은 **빌드 시점에 반영**되므로 값을 넣거나 바꾼 뒤 반드시 **재배포**해야 합니다.
  설정 전까지는 가짜 주소를 만들지 않도록 `sitemap.xml` 이 비어 있습니다.

---

## 4. HTTPS 와 주소 통일

견적문의로 이름·연락처·주소가 오가므로 **반드시 https 로 서비스**해야 합니다.
서버형이면 Let's Encrypt, 클라우드타입·Vercel 은 자동 발급됩니다.

`www.clearcheolgeo.com` 과 `clearcheolgeo.com` 중 **하나를 대표 주소로 정하고 나머지는 리다이렉트**하세요.
(검색엔진 중복 색인 방지) 대표 주소는 `NEXT_PUBLIC_SITE_URL` 과 일치시켜야 합니다.

---

## 5. 배포 후 점검

- [ ] `https://www.clearcheolgeo.com` 접속 확인 (자물쇠 표시)
- [ ] 휴대폰에서 하단 상담바 · 전화 버튼 동작 확인
- [ ] 견적문의 실제 접수 → `/admin` 에서 조회 확인
- [ ] `/admin` 상단이 "저장소: PostgreSQL · 연결 정상" 으로 표시되는지
- [ ] **재배포를 한 번 더 한 뒤** 기존 접수가 `/admin` 에 그대로 남아 있는지 ← 꼭 확인
- [ ] DB 자동 백업이 켜져 있는지 확인
- [ ] `https://www.clearcheolgeo.com/sitemap.xml` 생성 확인
- [ ] 구글 서치콘솔 / 네이버 서치어드바이저에 사이트 등록
- [ ] 테스트로 넣은 접수 기록 삭제
